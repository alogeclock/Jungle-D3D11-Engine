local Config = {
    forward_speed = 600.0,

    lane_width = 220.0,
    lane_count = 3,
    lane_change_speed = 900.0,

    gravity = -1800.0,
    jump_power = 760.0,
    slide_duration = 0.45,

    road_tile_length = 700.0,
    road_tile_count = 5,
    road_recycle_behind_distance = 700.0,

    obstacle_spawn_ahead_distance = 1600.0,
    obstacle_spacing = 900.0,
    obstacle_recycle_behind_distance = 500.0,

    camera_back_distance = 600.0,
    camera_height = 350.0,
    camera_follow_speed = 8.0,
    camera_look_ahead = 300.0,
    camera_look_height = 100.0,
}

return Config
